This directory contains two useful scripts related to the Span Similarity Dataset (SSD):

- `validate_dataset.py`: To ensure that the dataset file contains no error. Run `python validate_dataset.py --help` for more info.
- `get_dataset_statistics.py`: To obtain the dataset statistics, as reported in the paper. Run `python get_dataset_statistics.py --help` for more info.
